/**
 * Photographed walnut grain used by the Walnut finish — both the 3D PBR map and
 * the finish-picker swatch. Served from `public/` so the UI and TextureLoader
 * share one file.
 */
export const WALNUT_GRAIN_URL = '/walnut-grain.jpg';
