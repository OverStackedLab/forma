/**
 * Photographed oak grain used by the Oak finish — both the 3D PBR map and the
 * finish-picker swatch. Served from `public/` so the UI and TextureLoader share
 * one file.
 */
export const OAK_GRAIN_URL = '/oak-grain.jpg';
